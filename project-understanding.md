# {{PROJECT_NAME}} — Project Understanding

> This file describes **what this project is** and **who it serves**. Update it as the project takes shape. The DSO planning skills (`/dso:roadmap`, `/dso:brainstorm`, `/dso:preplanning`) read this file to understand context before generating epics, stories, and tasks.

## What is this project?

`{{PROJECT_NAME}}` is a Next.js + TypeScript prototype scaffolded from the Digital Service Orchestra (DSO) NextJS template. It is a starting point — the actual product, audience, and success definition are filled in by you as you go.

## Who is the user?

*To be filled in. Examples: "Citizens applying for unemployment insurance in Ohio." or "Internal benefits caseworkers at a state agency."*

## What problem does it solve?

*To be filled in. One paragraph describing the gap between current-state and target-state for the user above.*

## What does success look like?

*To be filled in. Concrete, observable signals — e.g., "A first-time applicant can complete the form in under 8 minutes on a mobile phone with one hand." Not "users are happy" — measurable behavior.*

## Stack

- **Framework**: Next.js (App Router) + TypeScript
- **UI**: React + USWDS via `react-uswds`
- **Styling**: SCSS with USWDS theming
- **Testing**: Jest + React Testing Library; Storybook for components
- **i18n**: next-intl
- **Tooling**: ESLint, Prettier, pre-commit hooks, GitHub Actions CI

## Constraints

*To be filled in. Examples: "Must work on iOS Safari 15+ for accessibility audit." or "Must use only USWDS-approved components per state procurement contract."*

## Out of scope

*To be filled in. Examples: "Authentication — handled by the agency's existing SSO." or "Payment processing — out of scope for prototype."*

## Open questions

*Track unresolved decisions here so they don't get lost between sessions.*
